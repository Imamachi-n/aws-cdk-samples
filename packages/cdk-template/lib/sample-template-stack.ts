import { Stack, StackProps } from "aws-cdk-lib/core";
import { Construct } from "constructs";

/**
 * テンプレート CDK スタック
 */
export class SampleTemplateStack extends Stack {
  constructor(scope: Construct, id: string, props?: StackProps) {
    super(scope, id, props);

    // TODO: ここに記述
  }
}
